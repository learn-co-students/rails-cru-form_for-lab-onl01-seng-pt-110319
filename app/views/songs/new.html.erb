<h1>Create a Song</h1>

<%= form_for(@song) do |f| %>
	<%= f.label :name %>
	<p><%= f.text_area :name %></p>
	<%= f.label :artist_id %>
	<p><%= f.text_area :artist_id %></p>
	<%=f.label  :genre_id %>
	<p><%= f.text_area :genre_id %></p>
	<%= f.submit %>
	<%end%>