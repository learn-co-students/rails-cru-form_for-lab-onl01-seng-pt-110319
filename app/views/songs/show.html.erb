<h1>Song name: <%=@song.name%></h1>
<h2>Song genre: <%= link_to "#{@song.genre.name}",genre_path(@song.genre)%></h2>
<h2>Artist Name: <%= link_to "#{@song.artist.name}",artist_path(@song.artist)%></h2>
