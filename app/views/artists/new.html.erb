<h3>Artist Form</h3>

<%= form_for(@artist)  do |a| %> 
    <label>Artist's Name:</label>
    <%= a.text_field :name %><br>

    <label>Artist's Bio:</label>
    <%= a.text_area :bio %><br>

    <%= a.submit %>
    <% end %>

